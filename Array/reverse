#include <stdio.h>

int main() {
    int n, i, rev=0, digit=0;

printf("Enter the number:");
scanf("%d", &n);

    while(n!=0)
    {
        digit=n%10;
        rev = rev*10+digit;
        n=n/10;
    }

printf("%d is the reverse", rev);

    return 0;
}

